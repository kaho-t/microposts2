<% if current_user.like?(micropost) %>
  <%= form_with(model: current_user.favorites.find_by(micropost_id: micropost.id), local: true, method: :delete) do |f| %>
    <%= hidden_field_tag :micropost_id, micropost.id %>
    <%= f.button type: 'submit', class: 'btn btn-default' do %>
      <i class="fas fa-heart"></i> <%= micropost.liked_users.count %>
    <% end %>
  <% end %>
<% else %>
  <%= form_with(model: current_user.favorites.build, local: true) do |f| %>
    <%= hidden_field_tag :micropost_id, micropost.id %>
    <%= f.button type: 'submit', class: 'btn btn-default' do %>
      <i class="far fa-heart"></i> <%= micropost.liked_users.count %>
    <% end %>
  <% end %>
<% end %>